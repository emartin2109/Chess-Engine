/*
** EPITECH PROJECT, 2023
** Chess-Engine
** File description:
** ai
*/

#ifndef AI_H_
    #define AI_H_
    #include "struct.h"

    // an ai that actually touch grass
    recursive_params_t randy(int number_of_moves, recursive_params_t *move_list);
    int random_number(int min, int max);

    // an ai that actually
    recursive_params_t nerd();

#endif /* !AI_H_ */
